# Opcional: só se a API estiver em OUTRO domínio (ex.: segundo projeto na Vercel).
# Um único projeto na raiz do repo: deixe vazio ou não defina — o app usa a mesma origem.
# Local: use .env.development (ex.: http://127.0.0.1:8080)
VITE_API_URL=
