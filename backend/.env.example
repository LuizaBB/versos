DATABASE_URL=postgresql+psycopg://versos:versos@localhost:5432/versos
SECRET_KEY=change-me-in-dev-use-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Vercel (serviço backend com prefixo /_/backend) — opcional; se vazio e VERCEL=1, usa /_/backend
# BACKEND_ROUTE_PREFIX=/_/backend
# CORS_ORIGINS=https://meu-app.vercel.app
# CORS_ORIGIN_REGEX=   # vazio no .env local: o código usa o padrão *.vercel.app
